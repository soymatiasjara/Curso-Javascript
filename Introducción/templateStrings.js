const nombre = 'Matías';
const edad = 27;
const pais = 'Chile';

console.log('La persona es ' + nombre + ' tiene ' + edad + ' y él es de ' + pais);

// Usando Back Sticks ``
console.log(`La persona es ${nombre} es nombre tiene ${edad} y es de ${pais}`);
